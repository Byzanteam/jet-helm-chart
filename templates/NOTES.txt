Please visit the url:
{{- $path := (.Values.jetSubpath | default "") }}
{{- range $_, $host := .Values.hosts }}
  {{ empty $.Values.certificate | ternary "http" "https" }}://{{ $host }}{{ $path }}
  {{ empty $.Values.certificate | ternary "http" "https" }}://{{ $host }}{{ $path }}/breeze
{{- end }}
